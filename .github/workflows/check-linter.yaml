name: Check linter in PRs
on:
  pull_request:
    types: [opened,edited,reopened,synchronize]
jobs:
  linter:
    runs-on: ubuntu-latest
    steps:
      - name: Run yarn
        run: yarn
      - name: Linter
        run: yarn lint
